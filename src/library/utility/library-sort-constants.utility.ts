import { LibrarySortOrder } from '../model/enum';

/** The default sorting order for libraries if not specified */
export const LIBRARY_DEFAULT_SORT_ORDER: LibrarySortOrder =
  LibrarySortOrder.CREATED_AT;
