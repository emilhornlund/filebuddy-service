/** The minimum allowed page number */
export const MINIMUM_PAGE_NUMBER: number = 0;

/** The maximum allowed page number */
export const MAXIMUM_PAGE_NUMBER: number = Number.MAX_SAFE_INTEGER;

/** The default page number if not specified */
export const DEFAULT_PAGE_NUMBER: number = MINIMUM_PAGE_NUMBER;

/** The minimum allowed number of results per page */
export const MINIMUM_PAGE_SIZE: number = 10;

/** The maximum allowed number of results per page */
export const MAXIMUM_PAGE_SIZE: number = 50;

/** The default number of results per page if not specified */
export const DEFAULT_PAGE_SIZE: number = MINIMUM_PAGE_SIZE;
